@model View.Models.ImprovementViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div>
    <h4>ImprovementViewModel</h4>
    <hr />

         @foreach (var tag in Model.Returned as List<Core.Classes.DTO.TagAndAmount>)
                {
                    <p> @tag.count </p>

                    <p>@tag.tag.name</p>
                }

        change settings:

        <form method="GET">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @foreach (var tag in ViewBag.Tags as List<Core.Classes.Models.Tag>)
            {
                <div>
                    <label>
                        <input type="checkbox" name="usedTags" value="@tag.tagId" />
                        @tag.name
                    </label>
                </div>
            }

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>

    <dl class="row">
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
